all:
  vars:
      ansible_network_os: cisco.ios.ios
      ansible_connection: network_cli
  children:
    routers:
      hosts:
        R1:
          ansible_host: 192.168.122.71
    switches:
      hosts:
        SW1:
          #ansible_host: 192.168.122.72 #I am going to check if I can give this an IP address in the 122 subnet. I will check if I can ping it from the host machine. If not, I will give it an IP address in the 10.10.27.0 subnet. 